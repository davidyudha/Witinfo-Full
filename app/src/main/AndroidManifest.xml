<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.witinfo">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="papb1"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.detail.DetailTanaman"></activity>
        <activity android:name=".ui.login.SignUp"></activity>
        <activity android:name=".ui.login.Reset"></activity>
        <activity android:name=".ui.kategori_buah.KategoriBuah"></activity>
        <activity android:name=".ui.kategori_hias.KategoriHias"></activity>
        <activity android:name=".ui.login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>